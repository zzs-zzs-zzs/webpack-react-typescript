import React from "react"
import styled from "styled-components"

const NoFoundContainer = styled.div`
  font-size: 80px;
`

const NoFound = () => {
  return <NoFoundContainer>404</NoFoundContainer>
}

export default NoFound
